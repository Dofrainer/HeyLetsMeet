<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.white.comporting.heyletsmeet">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Loading">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Location_Select" android:label="위치 선택">
        </activity>
        <activity android:name=".Select_Middle_Position" android:label="중간 위치 선택">
        </activity>
        <activity android:name=".Select_Data" android:label="asdf">
        </activity>

    </application>

</manifest>